# 2026-04-30 开发记录

## 完成内容：按待开发清单全面开发

### 上午：双栏布局 + 客户详情改造（已完成）

### 下午：缺失功能补齐

#### 后端新增
- `CustomerReminder` 模型（customer_reminders表）：id, customer_id, advisor_id, content, reminder_at, is_done, created_at
- `GET /api/customers/check-phone?phone=xxx` - 号码查重
- `GET /api/customers/my-count` - 客户数含上限信息（400条上限）
- `PUT /api/customers/{cid}` - 更新时手机号重复检查
- `POST /api/customers/{cid}/to-must-follow` - 加入必跟进
- `POST /api/reminders` / `GET /api/reminders/upcoming` / `PUT /api/reminders/{rid}/done` / `DELETE /api/reminders/{rid}` - 备忘提醒CRUD

#### 前端新增/修改
- **MyCustomers.vue**：工具栏完整补齐（刷新/号码查重/备忘提醒/我的重要客户/主管点评/待还款）；客户数上限显示（进度条+badge）；号码查重对话框；备忘提醒对话框（选客户+内容+时间）；主管点评对话框；分页扩展10/20/50/100/200；repay_status还款状态筛选
- **Dashboard.vue**：工作台新增"待处理提醒"卡片，支持标记完成
- **CustomerPage.vue**：加入必跟进按钮；编辑客户按钮+弹窗表单；备忘记录Tab（remark_type=2）；`toMustFollow` API调用
- **Pool.vue/ImportantPool.vue/Redistribution.vue**：移除旧抽屉，改用Layout双栏布局（inject openCustomerDetail）
- **api/index.js**：新增 checkPhone, getMyCustomerCount, getUpcomingReminders, createReminder, markReminderDone, deleteReminder, toMustFollow

#### 修复
- Layout.vue：删除重复的 user/isManager/roleText computed定义（lines 290-292）
- Pool.vue：修复变量合并语法错误

#### 服务状态（2026-04-30 10:30）
- 后端：PID 9572，端口8080 ✅
- 前端：PID 9944，端口5173 ✅
- crm.db: customer_reminders表已创建 ✅
- 修复布局：左侧信息区 + 右侧备注区并排显示（flex布局）
- 新增3个Tab：保单信息、负债信息、需求信息（共9个Tab）
- 保单Tab：保险公司/类型/年缴/已缴年限/现金价值/是否可贷款
- 负债Tab：房贷/车贷/网贷/其他月还款，自动计算月负债合计
- 需求Tab：贷款用途/期限/紧迫程度/说明
- 备注改进：显示备注人姓名、可同时变更客户状态
- 全部信息Tab：分区展示所有信息

### 2. 离职人员数据分配（Team.vue 完整重写）
- Tab3 离职分配：卡片式展示每个离职员工的客户数量
- 一键转移：选择接收顾问后批量转移所有客户
- 恢复在职：可将离职员工恢复为在职
- Tab2 团队管理：新增"离职处理"按钮（禁用账号）
- 添加成员：联通真实 POST /api/users 接口
- 后端新增API：GET /api/team/offboard、POST /api/team/reassign-all、PUT /api/users/{uid}/status

### 3. 日志报表增强（Logs.vue 完整重写）
- 团队今日跟进统计：卡片式展示每位顾问今日跟进条数
- 按备注类型筛选（跟进/主管点评/备忘）
- CSV导出：带Authorization header的前端fetch下载
- 主管点评：弹窗为任意客户备注添加点评（remark_type=1）
- 点击客户名弹出详情抽屉
- 后端 /api/stats/remarks 新增 remark_type 过滤 + 权限过滤 + customer_id 字段

### 4. 数据统计完善（Dashboard.vue 完整重写）
- 新增客户来源分布饼图（/api/stats/source-dist）
- 本月统计卡片加进度条（对比目标）
- KPI卡片美化（图标+颜色）
- 后端新增：GET /api/stats/source-dist、GET /api/stats/status-dist、GET /api/stats/team-daily

### 后端其他改进
- /api/customers/{cid}/remarks：JOIN User表，返回 advisor_name
- /api/customers/{cid}/remarks POST：支持 new_status 参数（提交备注同时变更客户状态）
- GET /api/stats/remarks/export：导出CSV带UTF-8 BOM

### 验证（2026-04-30 续期验证）
- 后端启动：uvicorn backend.main:app --host 0.0.0.0 --port 8080 ✅
- 前端启动：node_modules/vite/bin/vite.js --host 0.0.0.0 --port 5173 ✅（不能用.bin/vite）
- API验证全部通过：
  - `/api/auth/login` ✅
  - `/api/stats/source-dist` ✅ 返回来源分布
  - `/api/stats/status-dist` ✅ 返回状态分布
  - `/api/stats/team-daily` ✅ 返回团队日报
  - `/api/team/offboard` ✅ 返回离职列表
  - `/api/stats/remarks` ✅ 返回日志列表（带customer_id）
- 前端预览：http://localhost:5173 ✅

## 本轮Bug核查与修复（2026-04-30 下午续）

### 发现并修复的Bug

| # | 严重度 | 文件 | 问题 | 修复 |
|---|--------|------|------|------|
| 1 | 严重 | CustomerPage.vue | 编辑对话框（line274）缺少`</el-dialog>`闭合，备忘对话框嵌套在编辑框内 | 添加`</el-dialog>`于line296 |
| 2 | 中等 | ImportantPool.vue | Refresh/Search图标未从@element-plus/icons-vue导入 | 添加`import { Refresh, Search } from '@element-plus/icons-vue'` |
| 3 | 轻微 | CustomerPage.vue | 未使用的`onBeforeUnmount`导入 | 移除该导入 |
| 4 | 完成 | MyCustomers.vue | hiddenColumns已读取但未应用到表格列 | 在城市/年龄/额度/芝麻分/信用卡5列添加`v-if="!hiddenColumns.includes('key')"` |

### 后端API核查（全部正常）
- POST /api/customers/import-file (line1560) ✅ 顶级定义，非嵌套
- PUT /api/users/{uid} (line556) ✅ 顶级定义
- GET /api/transfer-history (line1090) ✅ 顶级定义
- GET /api/important-pool (line1135) ✅ 支持keyword/status/star/loan_type参数
- GET/PUT /api/settings (line1417/1425) ✅ 隐藏列功能
- GET /api/customers (line605) ✅ 支持repay_status参数
- POST /api/users (line579) ✅ 创建用户正常

### 服务状态（2026-04-30 下午）
- 后端：http://localhost:8080 ✅
- 前端：http://localhost:5173 ✅
- 所有已知Bug已修复 ✅
